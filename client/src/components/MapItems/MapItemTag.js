const MapItemTag = ({ tagName }) => {
  return (
    <div className="w-fit text-xs border-[1px] rounded-full px-2 flex justify-center items-center">
      {tagName}
    </div>
  );
};

export default MapItemTag;
